<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd">

    <import resource="infrastructure-applicationContext.xml"/>

    <int:gateway id="getUsersGateway"
                 default-request-channel="getUsers"
                 service-interface="com.tinubu.first.pz.processor.user.UserProcessor"
                 default-reply-channel="usersResponse"/>

    <int:channel id="getUsers"/>
    <int:channel id="usersResponse" datatype="java.util.List"/>

    <int:chain input-channel="getUsers"
               output-channel="usersResponse">
        <int:service-activator ref="userService" method="getUsers"/>
        <int:transformer ref="userTransformer"/>
    </int:chain>

    <bean id="userTransformer" class="com.tinubu.first.pz.processor.user.UserTransformer"/>

</beans>